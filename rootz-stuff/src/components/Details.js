import React from 'react';

export default function Details() {
  return (
      <div style={{
            display: 'grid',
            placeContent: 'center',
            height: '100vh',
        }}>
            <h1>Details</h1>
            <p>learning project, you can ignore it!</p>
        </div>
  );
}
