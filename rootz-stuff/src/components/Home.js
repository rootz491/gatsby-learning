import React from 'react';

export default function Home() {
    return (
        <div style={{
            display: 'grid',
            placeContent: 'center',
            height: '100vh',
        }}>
            <h1>Home</h1>
            <p>This is client only route & base path</p>
        </div>
    );
}
